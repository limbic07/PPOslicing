# 1. PyTorch with CUDA 12.1 (Best for RTX 3060 and WSL2/Ubuntu)
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.1+cu121
torchvision==0.18.1+cu121
torchaudio==2.3.1+cu121

# 2. Reinforcement Learning Frameworks
# Ray 2.34.0 is stable for RL Module API Stack without massive memory leaks
ray[rllib,tune]==2.34.0
gymnasium==0.28.1

# 3. Scientific Computing and Math (Lock numpy < 2.0 to prevent compatibility issues with Ray)
numpy>=1.26.0,<2.0.0
matplotlib==3.9.0

# 4. Utilities
gputil==1.4.0
tensorboard==2.17.0
pandas==2.2.2
